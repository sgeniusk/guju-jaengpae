# 세션 핸드오프 — Codex 이관용

이 문서는 **Codex CLI가 다음 세션을 이어받기 위한 진입점**이다. 에이전트-중립 규칙은 [AGENTS.md](AGENTS.md), 세계관 정본은 [docs/worldview.md](docs/worldview.md), 구조 이력은 [CHANGELOG.md](CHANGELOG.md).

## 현재 상태 (2026-06-06) — feat-055 보상 선택 비교 UX 완료
`./init.sh` 카드 **22개 / 2797 단언 green**. `tools/long_run_smoke.gd`가 유비·조조·손권 모두 stage 15 최종 보스까지 통과하고, `tools/ui_feedback_smoke.gd`가 첫 전투 수동 조작 경로, 기본 x3 속도, 상점 추천순 첫 카드, 전투 시작 rally banner/charge line/clash pulse, 피격 spark/crit/burst VFX, 보상 선택 `추천 —`/`비교 —` 문구와 tooltip을 검증한다. `tools/resume_ux_smoke.gd`는 no-save, corrupt-save 안내, valid-save 이어하기 버튼/tooltip, run_map route, stage/성/보드/손패/골드 복원을 검증한다. Godot 4.6.3 기준 군주 3 검증, run_map/lord_select/battle/보스 stage/결과 화면/UI 피드백/저장 이어하기/첫 5스테이지 플레이테스트 루프/장기런 스모크가 통과했다. feat-038에서 런 전투를 성 위치 선택→손패 3장 중 1장 배치 또는 계략 사용→즉시 단일 교전 루프로 바꿨고, feat-039에서 유닛 카드를 분대/증원 성장 단위로 바꿨으며, feat-040에서 이 루프를 MVP 제품 계약과 순수 helper/자동 메트릭으로 고정했다. feat-041은 첫 전투 적 전열과 시작 돌격 피드백을, feat-042는 격자 배치 전술 보너스와 태그 표시를, feat-043은 손패 선택 중 빈 타일 전술 미리보기를, feat-044는 stage 15 최종 보스까지 자동 장기런 검증을, feat-045는 전투 중 집중표적 명령 피드백을, feat-046은 상점/전리품 카드 선택의 전략 추천 문구를, feat-047은 현세 3군주 장기런 자동 검증을, feat-048은 첫 수동 플레이 smoke를, feat-049는 기본 전투 x3과 초반 시간 예산을, feat-050은 카드 선택 추천순 정렬을, feat-051은 저장/이어하기 UX smoke를, feat-052는 분대 밀도와 전투 시작 함성/VFX 체감을, feat-053은 데미지 이벤트마다 반복되는 피격 VFX를, feat-054는 손상 저장 이어하기 보호를, feat-055는 보상 선택 비교 UX를 추가했다.
- **feat-055 done** — `docs/specs/feat-055-reward-choice-comparison.md` 추가. `CardChoiceAdvisor`가 현재 보드/손패 기준으로 유닛 증원, 새 장수/병종, 건물, 계략, 보패의 선택 전후 변화를 `비교 — ...` 문구와 tooltip으로 계산한다. `battle.gd` 전리품 버튼은 `추천 — ...` 아래 `비교 — ...`를 표시하고 줄 수에 맞춰 버튼 최소 높이를 보정한다. UI smoke가 전투 보상 화면 비교 문구 렌더를 검증한다.
- **feat-054 done** — `docs/specs/feat-054-corrupt-save-resume-guard.md` 추가. `RunManager.run_save_status()`와 `has_resumeable_run_save()`가 파일 존재, ConfigFile payload, RunState 호환성, started 여부를 검사한다. `lord_select.gd`는 로드 가능한 저장만 이어하기로 표시하고, 손상/호환 불가 저장은 비활성 안내와 새 런 복구 tooltip으로 처리한다. `run_map.gd` 자동 로드도 재개 가능한 저장만 시도한다.
- **feat-053 done** — `docs/specs/feat-053-hit-impact-feedback.md`와 `BattleHitFeedback` helper 추가. 일반 피해는 spark, 치명타는 crit ring, 스킬/계략은 burst VFX 프로필로 변환한다. `battle.gd`는 `_play_damage_events()`마다 `hit_impact_vfx` meta Polygon2D를 VFX layer에 표시하고, UI smoke가 첫 수동 전투에서 spark/crit/burst 생성을 검증한다.
- **feat-052 done** — `docs/specs/feat-052-force-density-and-rally.md` 추가. `BattleFeel`/`FormationRenderer`의 병종 분대 렌더·visible cap을 18명, 장수 호위 cap을 10명으로 올렸다. `AudioManager`는 `rally` SFX cue를 기존 `battle_start.wav`에 연결한다. `battle.gd`는 전투 시작에 meta 태그가 있는 rally banner, charge line, clash pulse를 만들고 UI smoke가 첫 수동 플레이에서 이를 검증한다.
- **feat-051 done** — `docs/specs/feat-051-resume-ux-smoke.md`와 `tools/resume_ux_smoke.gd` 추가. 기본 저장 파일이 없을 때 lord_select에 이어하기 버튼이 보이지 않고, autosave 런이 있으면 `저장된 런 이어하기` 버튼/tooltip이 보인다. 버튼 핸들러는 `GameManager.change_scene("res://scenes/screens/run_map.tscn")` route를 발생시키고, `RunManager.load_run()`으로 stage/성 위치/보드/손패/골드를 복원한다. `init.sh` 표준 검증에 연결했다.
- **feat-050 done** — `docs/specs/feat-050-card-choice-priority.md` 추가. `CardChoiceAdvisor.ranked_ids()`가 기존 후보 id 배열을 추천 점수순으로 stable sort한다. `run_map.gd` 상점 카드와 `battle.gd` 전리품 버튼이 이 순서로 렌더된다. 후보 생성·확률·unlock 정책은 그대로이며 보이는 순서만 전략적으로 정렬한다.
- **feat-049 done** — `docs/specs/feat-049-battle-tempo-budget.md` 추가. `battle.gd` 기본 전투 속도는 x3이고, UI smoke가 x3 버튼 선택 상태를 검증한다. `PlaytestMetrics.first_five_ok()`는 첫 5스테이지 전투를 개별 24초, 평균 20초 이하로 제한하며 `test_fun_contract.gd`가 느린 개별 전투와 느린 평균 전투를 회귀 테스트로 잡는다.
- **feat-048 done** — `docs/specs/feat-048-manual-playthrough-smoke.md` 추가. `tools/ui_feedback_smoke.gd`가 첫 손패를 계략/보병/건물로 고정하고, 성 위치 선택→계략 타일 배치 거부→보병 배치→즉시 교전 시작을 실제 `battle.tscn` 메서드로 검증한다. 교전 시작 후 성 위치, 보드 배치, 손패 감소, 교전당 1장 제한, 전투 phase, 성/아군/적 유닛 생성, `전군 돌격!` hint를 확인한다.
- **feat-047 done** — `docs/specs/feat-047-three-lord-long-run-smoke.md` 추가. `tools/long_run_smoke.gd`가 유비·조조·손권을 각각 새 `RunState`로 시작해 stage 1~15를 결정적으로 진행한다. 선택기는 유닛 배치/증원, 건물 배치, 망루 오라, 계략 battle/run 효과, 병법서 보패를 실제 시뮬레이션에 반영한다. 조조 첫 정예에서 막히던 명궁 스파이크는 `WaveFactory` 정예 명궁 수치를 좁게 낮췄고, `test_wave_factory.gd`가 stage 7 encounter 수치를 검증한다. 손권은 주유 우선 루트로 오나라 화공 축을 검증한다.
- **feat-046 done** — `docs/specs/feat-046-card-choice-advisor.md`와 `CardChoiceAdvisor` helper/test 추가. `run_map.gd` 상점 카드와 `battle.gd` 전리품 버튼이 현재 보드/레벨/손패/골드 기준으로 `추천 — 증원 후보`, `추천 — 경제 확장`, `추천 — 자금 부족` 같은 visible text와 tooltip을 표시한다. `tools/ui_feedback_smoke.gd`가 상점과 보상 화면에서 추천 문구 렌더를 검증한다.
- **feat-045 done** — `docs/specs/feat-045-command-feedback.md`와 `BattleCommandFeedback` helper/test 추가. `battle.gd`는 집중표적 버튼 tooltip에 전투 상태/현재 표적/장수 수를 표시하고, 적 클릭 성공 시 표적명·장수 수 힌트, 표적 위 `집중` 라벨, 장수→표적 지휘선 VFX를 보여준다. 빈 곳 클릭과 표적 사망은 자동 표적 복귀 문구를 남긴다. `tools/ui_feedback_smoke.gd`가 실제 전투 단계에서 집중표적 피드백을 검증한다.
- **feat-044 done** — `docs/specs/feat-044-long-run-smoke.md`와 `tools/long_run_smoke.gd` 추가. 유비 기준 stage 1~15 결정적 선택으로 전투·보스·칙령·상점·사건·확장 노드를 통과한다. 병법서 보패를 장착하고 `TreasureCatalog` battle attack 보정을 시뮬레이션에 적용해 stage 15 최종 보스까지 승리 가능성을 검증한다. `init.sh`에 연결됨.
- **feat-043 done** — `docs/specs/feat-043-formation-preview.md` 추가. `FormationTactics.preview_for_unit()`이 전술 태그와 공격 보너스를 `엄호 +15%` 같은 표시 문자열로 바꾸고, battle.gd는 선택 손패 기준 임시 보드를 계산해 빈 타일 label/tooltip에 preview를 표시한다. `tools/ui_feedback_smoke.gd`가 전투 씬에서 궁병 엄호 미리보기를 검증한다.
- **feat-042 done** — `docs/specs/feat-042-formation-tactics.md`와 `FormationTactics` helper/test 추가. 병종이 상하좌우 인접 장수에게 지휘를 받으면 공격 +10%, 원거리 유닛 앞 같은 열에 근접 아군이 있으면 엄호 +15%, 기병이 좌우 가장자리 열에 있으면 측면 +10%를 받는다. `CardCatalog.build_board_army()`가 실제 유닛 공격력에 적용하고 battle.gd는 배치 타일 라벨에 `지휘/엄호/측면` 태그를 붙인다. 전술 재계산은 base attack 메타 기준이라 중첩 곱셈하지 않는다.
- **feat-041 done** — `docs/specs/feat-041-battle-feel-pass.md`와 `BattleFeel` helper/test 추가. stage 1 normal encounter는 3개 저체력 적 분대가 중앙 전선 근처에 펼쳐져 enemy visible 25명으로 보인다. `battle.gd`는 전투 시작 순간 "전군 돌격!" banner, 양 진영 charge line, 짧은 camera shake를 뷰 레이어에만 표시한다. `PlaytestMetrics`는 아군/적/전체 visible soldiers를 출력한다. 플레이테스트 루프는 stage 1/2/5 전투 21.1s/18.3s/14.6s로 통과했다.
- **feat-040 done** — `docs/specs/feat-040-fun-reset.md` 추가. `StrategyDeckCatalog`가 3국 12장 전략 풀을 정의하고 첫 손패는 장수+병종 혼합 3장이다. `SquadProfile`은 병종 8~10명 분대와 장수 호위병 성장을, `TerrainPerkCatalog`는 촉/위/오 지형 특전을, `FormationRenderer`는 분대/호위 오프셋을, `PlaytestMetrics`는 첫 5스테이지 루프 증거를 맡는다. `CardUiText.deploy_action_label`은 배치/증원/건물/계략 의도를 실제 손패 UI에 드러내고, 우물은 성/보드 군세/한 장 제한을 지키는 한 수로 계산된다.
- **feat-039 done** — `RunState.board_levels`와 save_version 1.2.0을 추가했다. 같은 유닛 카드는 `RunManager.upgrade_from_hand`로 기존 보드 칸 Lv.+1 증원이 되며 새 칸을 차지하지 않는다. `RewardPool`은 유닛 카드를 Lv.5까지 다시 제안하고, `CardCatalog`는 병종 squad_count/레벨 성장과 장수 retinue_count를 전투 유닛에 심는다. `battle.gd`는 병종 8~14명 분대 렌더, 장수 축소 본체+호위병, 기본 속도 x2를 적용한다. `BattleSim`은 근접/원거리 교전 거리를 늘려 걷는 시간을 줄였다.
- **feat-038 done** — `RunState`는 9장 전략 덱/3장 손패/draw_pile/castle_key/terrain_perk_id/deploy_cards_played를 저장한다. `RunManager`는 성 선점과 교전당 1장 제한을 강제하고, `BattleSim.add_castle_at`은 선택 타일에 성을 세운다. `WaveFactory.stage_encounter_waves`는 실제 런 전투를 단일 encounter로 만든다. `CardCatalog`는 촉=성 인접 체력, 위=성 행 공격, 오=가장자리 공격 지형 특전을 적용한다. `battle.gd`는 성을 먼저 고르게 하고 카드 1장 배치 즉시 `교전 시작`으로 넘어가며, 유닛 아래 그림자를 렌더한다.
- **v0.6 done** — feat-029(위·오 trait·스킬)·feat-020(땅 확장)·feat-021(왕의 칙령)·feat-028(유닛 애니). 각 architect APPROVED + 회귀 검증.
- **Codex 이관 후 진행** — `.omx/ultragoal` 장부 생성, G001~G018 원칙·범위 checkpoint 완료, G019 push는 사용자 확인 대기로 failed 기록, G020 `.import` 정책 완료.
- **Phase 1 완료** — feat-031 스펙 추가, G026 StageCadence 첫 15스테이지 node_kind baseline 테스트 추가, G027 trait 설명 정합성 완료, G028 lord별 시각 QA 루틴 완료, G029 첫 보스 런 플로우 sanity 완료, G030 첫 5스테이지 UI 스크린샷 완료, G031~G032 증거 정리 완료, G033 최종 밸런스 Phase 7 이월 명시 완료. `tools/shoot_visual_qa.sh`가 `/tmp/guju-visual-qa-smoke`에 10 PNG를 생성하고, `tools/shoot_run_flow.sh`가 `/tmp/guju-run-flow-qa-smoke`에 위·촉·오 run_map stage 1/3/4/5 12 PNG를 생성한다. `test_run_flow_sanity.gd`는 위·촉·오 stage 1→2→칙령→상점→첫 보스→확장→stage 6 흐름을 잠근다. G029의 수치 조정은 blocker 보정이었고, Phase 7 G078에서 수치 계약 잠금을 시작했다.
- **Phase 2 완료** — feat-032/G034에서 기존 `CardData.card_type`의 `scheme`/`treasure` 값을 유지하고, `SchemeCardData`/`TreasureCardData`를 `effect_id` 중심 Resource subclass 계약으로 확정했다. G035에서 계략은 `RunState.hand`에서 `RunManager.cast_scheme_from_hand`로 발동 후 소비되고, battle.gd는 계략 발동 버튼과 타입 라벨로 유닛/건물 배치와 분기한다. G036에서 `SchemeCatalog`가 RNG/I/O 없이 `battle`/`run` 결과 딕셔너리를 반환한다. G037에서 보패는 `RunState.treasures`에 장착되고, `TreasureCatalog`가 battle/economy/reward 보정 딕셔너리를 합산한다. G038에서 `RewardPool`은 기본 전리 타입 general/troop/scheme/treasure, 명시 building policy, 보패 stack_limit을 반영한다. G039에서 `tools/validate_cards.gd`가 scheme/treasure subclass, effect registry, cost, stack policy를 검사한다. G040에서 계략 `scheme_raid`/`scheme_levy`/`scheme_fortify`, 보패 `treasure_bingfashu`/`treasure_jinyin`/`treasure_qianliyan` 리소스가 추가됐다. G041에서 계략 런 골드·성 보강·적 피해와 보패 아군 공격·건물 골드·보상 후보 수가 실제 런/전투/보상 결과에 연결됐다. G042에서 `RunState` board/hand/edicts/treasures/owned/scalars가 id/primitive 값만 담는 회귀 테스트가 추가됐다. G043에서 mixed hand 장수·병종·건물 배치, 군세 변환, 건물경제, 계략 발동 분기가 유지되는지 검증했다. G044에서 `CardUiText`로 계략 발동, 보패 장착, 보드/건물 배치 UI 문구를 통일했고 상점 목록을 스크롤 영역으로 정리했다. G044 상점 캡처는 `/tmp/guju-g044-ui/shop_lord_liubei_stage_4.png`.
- **Phase 3 완료 / Phase 4 게이트 / Phase 5 완료 / Phase 6 완료 / Phase 7 완료** — feat-033/G045~G053에서 ConfigFile 저장 포맷, RunState/ProfileState primitive payload, save_version, ProfileState API, 결과 화면 기록·해금·새 런 경로, 런 autosave/이어하기, 신규/호환/미래 버전 테스트, unlock-aware 보상/군주 선택, 프로필 저장/로드, 저장 I/O 경계를 닫았다. G054에서 `docs/specs/feat-034.md`를 추가하고 `docs/worldview.md`의 천계·마계 nation id를 사용자 승인 후 Phase 4 확장으로 명시했으며, `test_nine_faction_gate.gd`가 CardVocab 현세 3국 제한과 정본 승인 순서를 검증한다. G055/G056은 명칭 승인 대기 blocked로 Ultragoal ledger에 남겼다. G057에서 `CardCatalog`/`CardLibrary` 군주 목록 API를 추가하고 `lord_select`를 3군주 고정 배열에서 카탈로그 기반 unlock-aware UI로 바꿨다. G063은 현재 Resource schema와 validator가 승인 정본과 일치함을 ledger로 닫았다. G064~G070에서 act-aware WaveFactory, 보스 3종, 정예/사건 node_kind, 최종 보스 승리 조건, 첫 15스테이지 런 믹스, stage 5/10/15 보스별 순수 sim·battle.tscn 부팅 스모크, 패배/최종 승리 결과 화면 스모크를 닫았다. G071~G077에서 `docs/specs/feat-036.md`, UI tooltip/피드백, walk 시트 26개, realm/stage 배경, 최소 오디오, 첫 전투·보상 온보딩, HUD placeholder 감소, `docs/reports/phase6-ui-screens/` 26 PNG 묶음을 남겼다. G078에서 `docs/specs/feat-037.md`와 `test_balance.gd`를 추가하고, 난이도 step 0.10, 칙령 10/20/15%, 둔전·망루·징발·보패 수치 계약을 잠갔다. G079에서 `export_presets.cfg`의 `macOS Desktop` preset, `.gitignore` export preset 추적 정책, `test_export_preset.gd`를 추가하고 pack export를 검증했다. G080에서 README, CHANGELOG, worldview, asset manifest, session-handoff, progress, feature_list를 릴리스 기준으로 맞췄고, G081에서 태그 후보와 release gate를 `docs/release-checklist.md`에 정리했으며, G082에서 로컬 fresh clone `./init.sh` green, G083에서 `build/macos/guju-jaengpae.zip` full app export와 export 앱 첫 전투 marker, G084에서 `docs/release-risks.md` 리스크 문서를 확인했다.
- **Codex 운영 문서 완료** — G085~G114에서 `docs/agent-routing.md`에 repo lookup, effort 기준, ralph fallback, ultragoal checkpoint loop, Codex App fallback, worker evidence, leader/verifier 책임, state-file ownership, Lore commit protocol, durable ledger, team/performance/research/ralph 사용 경계를 추가했다. 각 story는 개별 커밋과 `./init.sh` 2375/2375 green으로 닫혔다.
- **남은 Ultragoal 상태** — `.omx/ultragoal/goals.json` 기준 complete 107, failed 3, pending 4. G019는 push 사용자 확인 대기, G055/G058은 canon 승인 전 구현 불가라 failed audit trail로 남겼고, G056/G060/G061/G062는 천계·마계 nation id/resource 승인 전 pending blocked다.
- **현재 local history** — 현재 작업 브랜치는 `codex/feat-040-mvp`다. feat-040 MVP 기준점은 `5a10dda feat: lock fun reset MVP`, feat-041은 `46a882a feat: improve first battle feel`, feat-042는 `dd923f4 feat: add formation tactics`, feat-043은 `8706667 feat: preview formation bonuses`, feat-044는 `ed4425f feat: add long run smoke`, feat-045는 `fce9dd5 feat: improve command target feedback`, feat-046은 `1903b7d feat: add card choice advisor`, feat-047은 `4e1dc8b feat: expand long run smoke to three lords`, feat-048은 `4b986e2 feat: add manual playthrough smoke`, feat-049는 `3d0ad9f feat: tighten battle tempo budget`, feat-050은 `13216d8 feat: sort card choices by recommendation`, feat-051은 `ec1f127 feat: add resume UX smoke`, feat-052는 `d8984fd feat: improve force density and rally feel`, feat-053은 `17ea5e7 feat: add hit impact feedback`, feat-054는 `3a1f29a feat: guard corrupt resume saves`로 정리했다. feat-055는 최신 로컬 커밋으로 정리한다. push/tag는 사용자 확인 전 금지다.

## 가장 먼저 — 미푸시 커밋 push (사용자 확인 후)
```
git log --oneline origin/main..main   # 미푸시 전체 확인
git push origin main
```
v0.6 피처 커밋 + handoff 갱신 docs가 미푸시 상태다:
- `ca6b816` feat-029 위·오 진영 깊이
- `93de775` feat-020 땅 확장
- `449a2bf` feat-021 왕의 칙령
- `e7d7a6a` feat-028 유닛 애니메이션
- (+ handoff 갱신 docs 커밋 2개)

feat-027 세션 커밋은 이미 push됨. 정확한 미푸시 수는 `git log origin/main..main`로 확인.
사용자 확인 전에는 push하지 않는다. 확인이 없으면 보류 사유를 `progress.md`와 Ultragoal ledger에 남기고 로컬 개발을 계속한다.

## 분업 (Codex 이관 후)
- **이전 분업** — Claude(편집장·스펙/정본/QA) → Codex(GDScript 구현) → agy(애셋) → architect(검증).
- **Codex 이관 시** — Codex가 구현 주도. 새 피처는 `docs/specs/feat-0XX.md` 스펙을 먼저 쓰고(이 디렉토리에 feat-020/021/028/029 예시 있음, 같은 형식) 그대로 구현. 검증은 항상 `./init.sh` 전체 green + 단언 수 증가. 결정성(BattleSim 순수 로직) 보존.
- BattleSim·battle_unit·전투 로직 변경은 신중히(결정성 테스트가 잡는다). 뷰(battle.gd)·데이터(.tres)·런(run_state/run_manager)는 상대적으로 안전.

## 다음 작업 후보 (Codex가 골라 진행)
우선순위·난이도 순. 각각 `docs/specs/`에 스펙 먼저 쓰고 구현한다.
1. **명칭 승인 대기** — G055/G056/G058/G060/G061/G062는 천계·마계 nation id, 군주명, resource id가 사용자/편집장 정본으로 승인될 때까지 재개하지 않는다.
2. **완성판 안전 작업** — 전투 결과/보상 화면 선택 설명 강화, 저장 슬롯/삭제 UX, 보상 선택 후 다음 스테이지 준비 안내처럼 nation id 확정 없이 가능한 작업을 feat-055 이후 하나씩 고른다.
3. **Phase 7 — 릴리스 준비** — G078 밸런스 수치 계약, G079 macOS Desktop preset/pack export, G080 릴리스 문서 동기화, G081 태그·릴리스 체크리스트, G082 fresh clone 검증, G083 full app export 검증, G084 리스크 문서화는 완료됐다. push/tag는 사용자 확인 전 대기한다.

## 코드 구조 포인터 (Codex 진입)
- 전투 로직(순수·결정적) — `scripts/battle/battle_sim.gd`(ROW_X/COL_Y/성/이동/승패), `battle_unit.gd`(스탯/상태/effective_attack).
- 스킬 — `scripts/battle/skill_system.gd`(const+COOLDOWNS+cast match+`_cast_*`, `_record_damage_event` 필수). 9스킬(촉5+위오4).
- trait/edict — `scripts/resources/card_catalog.gd` `build_player_unit`(인덕 hp·호패/수전 atk·edict atk), `scripts/run/edict_catalog.gd`.
- 런 상태 — `scripts/run/run_state.gd`(board_rows 3~6·hand·gold·edicts·treasures·stage_index, primitive payload), `scripts/run/profile_state.gd`(프로필 primitive payload), `scripts/autoloads/run_manager.gd`(API·stage_node_kind·is_final_boss_stage·run_result outcome·run save/load/autosave·profile save/load/ensure·profile unlock 조회·profile-aware reward 후보), `scripts/run/stage_cadence.gd`(상점4·보스5·확장5·칙령3·정예7·사건11·최종보스15, node_kind 우선순위), `scripts/battle/wave_factory.gd`(5스테이지 단위 act-aware 파도·보스 템플릿), `scripts/battle/skill_system.gd`(보스 스킬 포함), `scripts/run/scheme_catalog.gd`, `scripts/run/treasure_catalog.gd`, `scripts/run/reward_pool.gd`(profile-aware eligible/roll), `scripts/run/persistence_store.gd`(ConfigFile 포맷/경로/run/profile section).
- feat-038 전술 배치 루프 — `scripts/run/run_state.gd`(3장 손패/draw_pile/castle_key/terrain_perk_id/deploy_cards_played), `scripts/autoloads/run_manager.gd`(성 선점·교전당 1장 제한·single encounter waves), `scripts/resources/card_catalog.gd`(9장 전략 덱과 terrain perk), `scripts/battle/wave_factory.gd`(`stage_encounter_waves`), `scripts/battle/battle_sim.gd`(`add_castle_at`), `scripts/battle/battle.gd`(성 먼저 선택·1장 배치 즉시 교전·그림자).
- feat-039 분대·증원 성장 — `scripts/run/run_state.gd`(`board_levels`, `upgrade_from_hand`, save 1.2.0), `scripts/autoloads/run_manager.gd`(`can_upgrade_from_hand`, `upgrade_from_hand`), `scripts/run/reward_pool.gd`(유닛 카드 Lv.5까지 반복 후보), `scripts/resources/card_catalog.gd`(`_apply_squad_growth`, squad_count/retinue_count), `scripts/battle/battle_unit.gd`(squad fields), `scripts/battle/battle.gd`(`_create_formation_body`, `_add_retinue_members`, 기본 `_speed=2.0`), `scripts/battle/battle_sim.gd`(교전 거리).
- feat-041 전투 체감 — `scripts/battle/battle_feel.gd`(visible soldiers/전열/rally text 순수 helper), `scripts/battle/wave_factory.gd`(stage 1 3분대 enemy front), `scripts/battle/battle.gd`(battle start rally banner/charge line/camera shake), `scripts/run/playtest_metrics.gd`(아군/적/전체 visible soldier metric), `test/test_battle_feel.gd`.
- feat-042 진형 전술 — `scripts/run/formation_tactics.gd`(지휘/엄호/측면 전술 보너스와 태그), `scripts/resources/card_catalog.gd`(build_board_army 전술 적용), `scripts/battle/battle.gd`(배치 중 재계산과 타일 라벨 태그), `test/test_formation_tactics.gd`.
- feat-043 전술 미리보기 — `scripts/run/formation_tactics.gd`(`preview_for_unit`, `preview_label`), `scripts/battle/battle.gd`(`_placement_preview_for_block`), `tools/ui_feedback_smoke.gd`(전투 전술 미리보기 OK).
- feat-047 장기런 스모크 — `tools/long_run_smoke.gd`(유비·조조·손권 stage 1~15 결정적 장기런, 건물/망루/계략/보패 반영), `scripts/battle/wave_factory.gd`(첫 정예 명궁 스파이크 완화), `scripts/run/strategy_deck_catalog.gd`(조조 첫 정예 전 망루 draw 순서), `test/test_wave_factory.gd`(stage 7 encounter 수치), `init.sh`(장기런 스모크 실행).
- feat-046 카드 선택 전략 안내 — `scripts/run/card_choice_advisor.gd`(보드/레벨/손패/골드 기반 추천 문구), `scripts/screens/run_map.gd`(상점 추천 라벨·tooltip), `scripts/battle/battle.gd`(전리품 추천 버튼), `test/test_card_choice_advisor.gd`, `tools/ui_feedback_smoke.gd`(상점·보상 추천 렌더 검증).
- feat-048 수동 플레이 QA — `tools/ui_feedback_smoke.gd`(성 선택, 계략 타일 배치 거부, 병종 배치, 즉시 교전 시작, 성/아군/적 생성 검증), `docs/specs/feat-048-manual-playthrough-smoke.md`.
- feat-049 전투 템포 — `scripts/battle/battle.gd`(기본 `_speed=3.0`), `scripts/run/playtest_metrics.gd`(첫 5스테이지 개별/평균 시간 예산), `test/test_fun_contract.gd`(느린 전투 회귀), `tools/ui_feedback_smoke.gd`(x3 기본 선택 검증).
- feat-050 카드 선택 정렬 — `scripts/run/card_choice_advisor.gd`(`ranked_ids` 추천 점수순 stable sort), `scripts/screens/run_map.gd`(상점 추천순), `scripts/battle/battle.gd`(전리품 추천순), `test/test_card_choice_advisor.gd`, `tools/ui_feedback_smoke.gd`(상점 첫 추천 카드 검증).
- feat-055 보상 선택 비교 UX — `scripts/run/card_choice_advisor.gd`(`comparison_for_card`, `comparison_line_for_card`, `comparison_tooltip_for_card`), `scripts/battle/battle.gd`(전리품 버튼 `추천`+`비교`, 줄 수 기반 버튼 높이), `test/test_card_choice_advisor.gd`, `tools/ui_feedback_smoke.gd`(보상 비교 렌더 검증).
- feat-051 저장/이어하기 UX — `tools/resume_ux_smoke.gd`(lord_select no-save/continue 버튼/route/load 복원 검증), `scripts/screens/lord_select.gd`(이어하기 버튼/핸들러), `scripts/autoloads/run_manager.gd`(save/load/autosave), `scripts/run/persistence_store.gd`(ConfigFile 경로), `init.sh`(표준 smoke 연결).
- feat-052 병력 밀도/함성 — `scripts/battle/battle_feel.gd`(visible cap/rally cue), `scripts/battle/formation_renderer.gd`(18명 분대/10명 호위 offset), `scripts/battle/battle.gd`(rally banner/charge line/clash pulse meta VFX), `scripts/autoloads/audio_manager.gd`(`rally` SFX), `tools/ui_feedback_smoke.gd`(첫 수동 플레이 VFX 검증), `test/test_battle_feel.gd`, `test/test_formation_renderer.gd`, `test/test_audio_manager.gd`.
- feat-053 피격 타격감 — `scripts/battle/battle_hit_feedback.gd`(damage event→spark/crit/burst profile), `scripts/battle/battle.gd`(`_spawn_hit_impact_vfx`, `hit_impact_vfx` meta), `tools/ui_feedback_smoke.gd`(첫 수동 전투 hit VFX 검증), `test/test_battle_hit_feedback.gd`.
- feat-054 손상 저장 보호 — `scripts/autoloads/run_manager.gd`(`run_save_status`, `has_resumeable_run_save`), `scripts/screens/lord_select.gd`(valid save 이어하기, corrupt save 안내), `scripts/screens/run_map.gd`(resumeable guard), `tools/resume_ux_smoke.gd`(no-save/corrupt-save/valid-save), `test/test_run_resume.gd`, `test/test_lord_select.gd`.
- 뷰 — `scripts/battle/battle.gd`(아이소 렌더·HUD·AnimatedSprite2D 분기·VFX·오디오 cue), `scripts/screens/run_map.gd`(상점·확장·칙령 드래프트 UI), `scripts/ui/card_ui_text.gd`(카드 타입·행동 경로 UI 문구), `scripts/autoloads/audio_manager.gd`(BGM/SFX 레지스트리).
- 카드 데이터 — `resources/cards/*.tres`, `resources/lords/*.tres`. 카탈로그 `scripts/resources/card_catalog.gd`, 오토로드 wrapper `scripts/autoloads/card_library.gd`. `lord_select`는 `CardLibrary.lord_list()`로 군주 버튼을 만든다.
- 테스트 — `test/test_*.gd`, 러너 `test/runner.gd`. `./init.sh`가 import+검증+스모크+테스트 일괄.
- QA 스크린샷 — `SHOT_DIR=/tmp/guju-visual-qa ./tools/shoot_visual_qa.sh`가 lord_select 1장 + 위·촉·오 battle deploy/fight/shop 9장을 생성. `./tools/shoot_ui_bundle.sh`는 기본으로 `docs/reports/phase6-ui-screens/`에 군주 선택, run_map, 전투, 상점, 패배, 최종 승리 26 PNG를 남기고 `tools/validate_screenshot_bundle.py`로 핵심 24장을 검증한다. 개별 실행은 `tools/shoot_battle.gd`(`LORD`·`SHOOT_STAGE`·`SHOOT_FORCE_RESULT` env), `tools/shoot_shop.gd`(`LORD`·`SHOP_STAGE` env), `tools/shoot_scene.gd`(`SCENE`·`SHOT_KIND` env).
- 운영 라우팅 — `docs/agent-routing.md`가 Codex 역할 경계를 기록한다. G085 기준 `explore`는 repo 파일·심볼·패턴 매핑 전용 read-only 역할이다.

## ⚠️ 운영 함정 (검증됨)
- **Codex stdin hang** — `codex exec "..."` 백그라운드는 `< /dev/null`로 stdin 닫을 것(안 닫으면 EOF 대기 hang).
- **agy 애셋** — `--add-dir <dir>` 주면 agy가 그 디렉토리에 이미지 직접 저장(brain 아님). "이미지만 생성, repo 파일 수정 금지" 명시(안 하면 feature_list/progress 자율편집).
- **PATH/HOME** — 백그라운드 셸 python3는 `/usr/bin/python3` 절대경로. godot용 `export HOME=.godot/home`과 PIL python을 한 셸에서 섞지 말 것(키잉 원 HOME, godot만 서브셸).
- 메모리 정본 — `agy-image-pipeline`(agy 파이프라인 상세).

## 알려진 사소 이슈
- feat-020 ROW_X 확장 행 전방 배치(공세 의도, 후방 성 공간 부족) — `battle_sim.gd` 주석.
- 칙령 stage 12=칙령·15=보스 우선(칙령 스킵 가능) — 의도된 충돌 처리.
- `init.sh` 부팅 스모크 라벨이 "run_map.tscn"인데 실제 main_scene은 lord_select — 혼동만, 무관.
- `docs/reports/v0.5-screens/*.png.import` — 보고용 스크린샷 import 사이드카이며 `.gitignore`에서 무시한다.
- 릴리스 리스크와 미지원 범위는 `docs/release-risks.md`에 고정했다.

## Codex 시작 프롬프트 (복사)
> 구주쟁패(/Users/taewookkim/dev/guju-jaengpae) 이어서. 현재 브랜치는 `codex/feat-040-mvp`. feat-038~040으로 성 선점→3장 손패→1장 플레이→분대/증원 성장 MVP 루프를 만들고, feat-041로 첫 전투 적 전열과 "전군 돌격!" 시작 VFX를 추가했으며, feat-042로 지휘/엄호/측면 진형 전술 보너스와 타일 태그를, feat-043으로 배치 전술 미리보기를, feat-044로 최종 보스까지 장기런 스모크를, feat-045로 전투 중 집중표적 피드백을, feat-046으로 상점/전리품 카드 선택 전략 추천을, feat-047로 유비·조조·손권 3군주 stage 15 장기런 스모크를, feat-048로 첫 전투 수동 플레이 smoke를, feat-049로 기본 전투 x3과 초반 시간 예산을, feat-050으로 상점/전리품 추천순 정렬을, feat-051로 저장/이어하기 UX smoke를, feat-052로 병력 밀도/함성 체감 패스를, feat-053으로 충돌 중 피격 spark/crit/burst VFX 반복을, feat-054로 손상 저장 이어하기 보호를, feat-055로 보상 선택 비교 UX를 추가했다. 최신 `./init.sh`는 카드 22개 / 2797 단언 green이고, 장기런은 3군주 모두 wins=8, board=5, rows=6으로 통과한다. UI smoke는 성 선택→계략 타일 배치 거부→보병 배치→전투 시작 경로, x3 기본 선택, 상점 첫 추천 카드, 전투 시작 rally banner/charge line/clash pulse, 피격 spark/crit/burst VFX, 전리품 `추천 —`/`비교 —` 렌더를 검증한다. Resume UX smoke는 no-save, corrupt-save 안내, valid-save 이어하기와 run_map route, stage/성/보드/손패/골드 복원을 검증한다. G055/G056/G058/G060/G061/G062는 명칭 승인 대기 blocked이므로 사용자/편집장 승인 전 천계·마계 nation id와 Resource를 추가하지 않는다. push/tag는 사용자 확인 전 금지. 다음은 전투 결과/보상 화면 선택 설명 강화, 저장 슬롯/삭제 UX, 보상 선택 후 다음 스테이지 준비 안내 중 하나를 잡아 `docs/specs/` 스펙 → 구현 → `./init.sh` green → 상태 파일 갱신 → 중요 커밋 순서로 진행한다.
